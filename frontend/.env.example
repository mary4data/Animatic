# Copy to .env and fill in for local dev. Vite only exposes vars prefixed
# VITE_ to client code, and this must never be hardcoded into the build --
# set it per-environment (local/.env, deployed platform's env config).
VITE_API_BASE_URL=http://localhost:8000
